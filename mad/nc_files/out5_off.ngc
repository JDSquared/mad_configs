(Remap subroutine to turn off output 5)
(P is 0 for synchronized output, immediate otherwise)
O<out5_off> sub

	O101 if [EXISTS[#<p>]] (Check if selecting output synchronization type)
		O102 if[#<p> NE 0]
			M65 P7
		O102 else
			M63 P7
		O102 endif
	O101 else
		M63 P7 (Default to motion synchronized)
	O101 endif

O<out5_off> endsub

M2 (If we ever get here stop the program execution)


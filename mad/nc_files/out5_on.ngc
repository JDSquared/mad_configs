(Remap subroutine to turn on output 5)
(P is 0 for synchronized output, immediate otherwise)
O<out5_on> sub

	O101 if [EXISTS[#<p>]] (Check if selecting output synchronization type)
		O102 if[#<p> NE 0]
			M64 P7
		O102 else
			M62 P7
		O102 endif
	O101 else
		M62 P7 (Default to motion synchronized)
	O101 endif

O<out5_on> endsub

M2 (If we ever get here stop the program execution)


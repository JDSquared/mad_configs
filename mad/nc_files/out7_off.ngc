(Remap subroutine to turn off output 7)
(P is 0 for synchronized output, immediate otherwise)
O<out7_off> sub

	O101 if [EXISTS[#<p>]] (Check if selecting output synchronization type)
		O102 if[#<p> NE 0]
			M65 P9
		O102 else
			M63 P9
		O102 endif
	O101 else
		M63 P9 (Default to motion synchronized)
	O101 endif

O<out7_off> endsub

M2 (If we ever get here stop the program execution)


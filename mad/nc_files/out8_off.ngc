(Remap subroutine to turn off output 8)
(P is 0 for synchronized output, immediate otherwise)
O<out8_off> sub

	O101 if [EXISTS[#<p>]] (Check if selecting output synchronization type)
		O102 if[#<p> NE 0]
			M65 P10
		O102 else
			M63 P10
		O102 endif
	O101 else
		M63 P10 (Default to motion synchronized)
	O101 endif

O<out8_off> endsub

M2 (If we ever get here stop the program execution)


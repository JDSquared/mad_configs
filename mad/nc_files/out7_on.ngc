(Remap subroutine to turn on output 7)
(P is 0 for synchronized output, immediate otherwise)
O<out7_on> sub

	O101 if [EXISTS[#<p>]] (Check if selecting output synchronization type)
		O102 if[#<p> NE 0]
			M64 P9
		O102 else
			M62 P9
		O102 endif
	O101 else
		M62 P9 (Default to motion synchronized)
	O101 endif

O<out7_on> endsub

M2 (If we ever get here stop the program execution)


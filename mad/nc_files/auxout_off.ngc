(Remap subroutine to turn off output P)
(P is 1-11)
(Q is 0 for synchronized output, immediate otherwise)
O<auxout_off> sub

	O101 if [EXISTS[#<q>]] (Check if selecting output synchronization type)
		O102 if[#<q> NE 0]
			M65 P[2 + #<P>]
		O102 else
			M63 P[2 + #<P>]
		O102 endif
	O101 else
		M63 P[2 + #<P>] (Default to motion synchronized)
	O101 endif

O<auxout_off> endsub

M2 (If we ever get here stop the program execution)

